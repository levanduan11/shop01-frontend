import { Routes, RouterModule, Route } from '@angular/router';
import { CartDetailComponent } from './cart-detail.component';

export const cartDetailRouting: Route = {
  path: 'cart-detail',
  component:CartDetailComponent
}
